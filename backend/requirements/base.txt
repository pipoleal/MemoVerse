Django
djangorestframework
django-cors-headers
django-filter

psycopg[binary]

# Parses DATABASE_URL (e.g. Render's managed Postgres) into Django's
# DATABASES dict. Only used when DATABASE_URL is set — local dev without it
# keeps using the SQLite config already in settings.py.
dj-database-url

python-decouple

Pillow

whitenoise

drf-spectacular

djangorestframework-simplejwt

# Cloudflare R2 exposes an S3-compatible API.  boto3 is used only by the
# backend to mint short-lived upload URLs and verify uploaded objects.
boto3

# Official Mercado Pago SDK, used exclusively by MercadoPagoClient
# (apps/payments/services/mercadopago_client.py) to talk to the Orders API.
mercadopago

# Official Resend SDK, used exclusively by apps/accounts/services/email_service.py
# to send password-reset codes. Only imported/called when RESEND_API_KEY is
# set — dev and tests use Django's own console/locmem email backend instead.
resend
