from django.apps import AppConfig


class RecoveryConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.recovery"
    verbose_name = "Recuperação de carrinho abandonado"
